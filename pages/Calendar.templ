package pages

import "net/http"

func Calendar(w http.ResponseWriter, req *http.Request) {
	T_Calendar().Render(req.Context(), w)
}

templ T_Calendar() {
	@Base_Nav() {
		<h1>Calendar</h1>
	}
}
